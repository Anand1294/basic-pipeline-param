pipeline {
    agent any
    stages {
        stage ('Greeting') {
            steps {
                echo "Hello, ${fullName}"
                echo "Your URL is: ${url}"
            }
        }
    }
}
